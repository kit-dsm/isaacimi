[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "isaacimi"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cerberus",
]

# [project.scripts]
# isaacimi = "isaacimi.main"